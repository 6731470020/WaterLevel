-- คุณภาพของค่าวัดแต่ละครั้ง
--
-- ค่าวัดบางครั้งใช้ได้แต่ "ไม่แน่นอน" เช่นเมื่อเสาวัดระดับทะลุขอบล่างของภาพ
-- บริการตรวจจับจะคืนตำแหน่งผิวน้ำเท่ากับความสูงภาพพอดี ซึ่งเป็นขอบเฟรม ไม่ใช่ผิวน้ำจริง
-- เดิมระบบทิ้งค่าเหล่านี้ไปเลย ทำให้ไม่มีข้อมูลอะไรเหลือเมื่อมุมกล้องไม่เหมาะ
-- จึงเปลี่ยนมาบันทึกไว้แต่ติดธงกำกับ ให้หน้าจอและรายงานบอกผู้ใช้ได้ว่าค่านี้เชื่อได้แค่ไหน
--
-- OK             = วัดได้ตามปกติ
-- AT_FRAME_EDGE  = ผิวน้ำอยู่ที่ขอบล่างของภาพ เป็นค่าประมาณขั้นต่ำเท่านั้น
ALTER TABLE measurements ADD COLUMN quality TEXT NOT NULL DEFAULT 'OK';

CREATE INDEX IF NOT EXISTS idx_measurements_quality
  ON measurements (station_id, quality, measured_at);
